<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
  <t t-name="owl.OwlOrangtuaDashboard" owl="1">
    <style>
      @media (max-width: 768px) {
        .controls-wrapper {
          flex-direction: column;
          width: 100%;
        }
        .header-text {
          font-size: 1.2rem;
        }

        .dateButton {
          width: 100%;
        }

        .timer-and-dropdown {
          display: flex;
          margin-top: 0.5rem;
          width: 100%;
          justify-content: between;
        }
        .dropdownButton {
          width: 100%;
        }
        .timer-button {
          width: 30%;
        }
      }

      .timer-and-dropdown {
        display: flex;
        margin: 1rem;
      }
    </style>
    <div class="container-fluid vh-100">
      <!-- Header Full Width -->
      <div class="mx-2 my-1">
        <div class="col-12">
          <div class="py-2">
            <div
              class="d-flex flex-wrap align-items-center justify-content-between"
            >
              <div class="d-flex">
                <h2 class="text-primary fw-bold header-text">
                  Dashboard Orangtua
                </h2>
              </div>
                <div class="controls-wrapper d-flex align-items-center mt-1 position-relative">
                <!-- Custom Date Range Button -->
                <div class="button-popup-wrapper">
                  <button
                  class="btn btn-outline-secondary dateButton"
                  t-on-click="toggleDatePicker"
                  >
                  <i class="fas fa-calendar"></i>
                  <t t-if="state.selectedDateRange">
                    <span
                    t-esc="formatDateRange(state.selectedDateRange.start, state.selectedDateRange.end)"
                    class="ms-2"
                    />
                  </t>
                  <t t-else=""> </t>
                  </button>
                  <!-- Pop-up langsung di bawah button -->
                  <div
                  t-if="state.showDatePicker"
                  class="popup-container bg-white p-3 rounded shadow-lg mt-2"
                  style="z-index:1050;"
                  >
                  <div class="d-flex align-items-center gap-3">
                    <div class="date-range d-flex flex-column">
                    <label class="date-label text-center">Dari</label>
                    <input
                      type="date"
                      class="form-control date-input"
                      t-model="state.tempDateRange.start"
                      t-on-change="applyDateRange"
                    />
                    </div>
                    <div class="date-range d-flex flex-column">
                    <label class="date-label text-center">Sampai</label>
                    <input
                      type="date"
                      class="form-control date-input"
                      t-model="state.tempDateRange.end"
                      t-on-change="applyDateRange"
                    />
                    </div>
                  </div>
                  </div>
                </div>
                <!-- Dropdown Periode -->
                <div class="timer-and-dropdown">
                  <div class="dropdown me-3 dropdownButton">
                    <button
                      class="btn btn-outline-secondary dropdown-toggle dropdownButton"
                      type="button"
                      data-bs-toggle="dropdown"
                      aria-expanded="false"
                    >
                      <span t-esc="getPeriodLabel(state.selectedPeriod)" />
                    </button>
                    <ul class="dropdown-menu dropdownButton">
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('bawaan')"
                        >
                          Default
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('bawaan')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('today')"
                        >
                          Hari Ini
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('today')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('yesterday')"
                        >
                          Kemarin
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('yesterday')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('thisWeek')"
                        >
                          Minggu Ini
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('thisWeek')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('lastWeek')"
                        >
                          Minggu Lalu
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('lastWeek')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('thisMonth')"
                        >
                          Bulan Ini
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('thisMonth')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('lastMonth')"
                        >
                          Bulan Lalu
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('lastMonth')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('thisYear')"
                        >
                          Tahun Ini
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('thisYear')}"
                          ></i>
                        </a>
                      </li>
                      <li>
                        <a
                          class="dropdown-item d-flex justify-content-between align-items-center"
                          href="#"
                          id="selectionFilter"
                          t-on-click.prevent="() => this.setPeriod('lastYear')"
                        >
                          Tahun Lalu
                          <i
                            class="fas fa-check"
                            t-att-class="{'invisible': !isPeriodSelected('lastYear')}"
                          ></i>
                        </a>
                      </li>
                    </ul>
                  </div>

                  <!-- Timer Button -->
                  <button
                    id="timerButton"
                    class="btn btn-outline-secondary timer-button"
                  >
                    <i id="timerIcon" class="fas fa-clock"></i>
                    <span id="timerCountdown"></span>
                  </button>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Date Range Picker Popup -->
      <!-- <div
        t-if="state.showDatePicker"
        class="position-fixed top-0 start-0 w-100 h-100 d-flex justify-content-center align-items-center"
        style="background: rgba(0, 0, 0, 0.5); z-index: 1050"
      >
        <div class="bg-white p-4 rounded shadow-lg">
          <div class="mb-3">
            <h5 class="mb-4">Pilih Rentang Tanggal</h5>
            <div class="mb-3">
              <label class="form-label">Dari</label>
              <input
                type="date"
                class="form-control"
                t-model="state.tempDateRange.start"
              />
            </div>
            <div class="mb-3">
              <label class="form-label">Sampai</label>
              <input
                type="date"
                class="form-control"
                t-model="state.tempDateRange.end"
              />
            </div>
          </div>
          <div class="d-flex justify-content-end gap-2">
            <button class="btn btn-secondary" t-on-click="closeDatePicker">
              Batal
            </button>
            <button class="btn btn-primary" t-on-click="applyDateRange">
              Terapkan
            </button>
          </div>
        </div>
      </div> -->

      <div class="mx-0 my-0">
        <OrangtuaKpiCard
          startDate="state.selectedDateRange?.start"
          endDate="state.selectedDateRange?.end"
        />
      </div>

      <!-- Bar Chart -->
      <div class="row mx-0 my-3">
        <div class="col-lg-12 col-md-12 col-sm-12 m-0 p-2">
          <div class="shadow-sm border p-4 bg-white h-100">
            <OrangtuaChartRenderer type="'bar'" title="'Tagihan Santri'" startDate="state.selectedDateRange?.start" endDate="state.selectedDateRange?.end" />
          </div>
        </div>
      </div>

      <div class="row mx-0 my-3">
        <!-- Pelanggaran Card -->
        <div class="col-lg-6 col-md-12 col-sm-12 m-0 p-2">
          <div
            class="shadow-sm bg-white h-100"
            style="border-radius: 0.375rem; overflow: hidden"
          >
            <OrangtuaCardList1
              startDate="state.selectedDateRange?.start"
              endDate="state.selectedDateRange?.end"
            />
          </div>
        </div>

        <!-- Tahfidz Card -->
        <div class="col-lg-6 col-md-12 col-sm-12 m-0 p-2">
          <div
            class="shadow-sm bg-white h-100"
            style="border-radius: 0.375rem; overflow: hidden"
          >
            <OrangtuaCardList2
              startDate="state.selectedDateRange?.start"
              endDate="state.selectedDateRange?.end"
            />
          </div>
        </div>
      </div>
    </div>
  </t>
</templates>
