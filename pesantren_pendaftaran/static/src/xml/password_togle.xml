<!-- <?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="pesantren_pendaftaran.PasswordToggleField" owl="1">
        <div class="o_field_password position-relative">
            <input t-ref="input"
                   t-att-type="inputType"
                   t-att-id="props.id"
                   t-att-name="props.name"
                   t-att-placeholder="props.placeholder"
                   t-att-value="props.value || ''"
                   t-att-autofocus="props.autofocus ? 'autofocus' : undefined"
                   t-att-readonly="props.readonly ? 'readonly' : undefined"
                   class="o_input" />
            <span class="position-absolute end-0 top-50 translate-middle-y pe-2" t-on-click="togglePassword">
                <i t-attf-class="fa {{state.showPassword ? 'fa-eye-slash' : 'fa-eye'}}"
                   style="cursor: pointer;"
                   t-att-title="state.showPassword ? 'Sembunyikan Password' : 'Tampilkan Password'"/>
            </span>
        </div>
    </t>
</templates> -->